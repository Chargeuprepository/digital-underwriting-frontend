import styled from 'styled-components';

const StyledVehicleInfoGridMaker = styled.div`
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
`;

export default StyledVehicleInfoGridMaker;
